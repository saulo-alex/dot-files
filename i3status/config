# i3status configuration file.
# see "man i3status" for documentation.

# It is important that this file is edited as UTF-8.
# The following line should contain a sharp s:
# ß
# If the above line is not correctly displayed, fix your editor first!

general {
        colors = true
        interval = 1
}

#order += "ipv6"
order += "wireless _first_"
order += "ethernet _first_"
order += "read_file file1"
order += "read_file file2"
order += "battery all"
order += "disk /"
order += "load"
order += "memory"
order += "volume internal"
order += "volume external"
order += "tztime local"

wireless _first_ {
        format_up = "W: %essid %ip"
        format_down = "W: down"
}

ethernet _first_ {
        format_up = "E: %ip (%speed)"
        format_down = "E: down"
}

battery all {
    format = "%status %emptytime"
    format_down = "No battery"
    status_chr = "⚡ CHR"
    status_bat = "🔋 BAT"
    status_unk = "? UNK"
    status_full = "☻ FULL"
    path = "/sys/class/power_supply/BAT%d/uevent"
    low_threshold = 10
}

disk "/" {
        format = "💾 %avail"
}

load {
        format = "%1min"
}

memory {
        format = "%used | %available"
        threshold_degraded = "1G"
        format_degraded = "MEMORY < %available"
}

tztime local {
        format = "%a, %d/%m %H:%M"
}

volume internal {
        format = "🔊 DELL: %volume"
        format_muted = "🎵: muted (%volume)"
        device = "pulse:alsa_output.pci-0000_00_1b.0.analog-stereo"
}

volume external {
        format = "🔊 Philips: %volume"
        format_muted = "🎵: muted (%volume)"
        device = "pulse:alsa_output.pci-0000_00_1b.0.hdmi-stereo"
}

read_file file1 {
    path = "/home/saulo/.local/share/backlight-external"
    format = "🔅 Philips: %content"
}

read_file file2 {
    path = "/home/saulo/.local/share/backlight-internal"
    format = "🔅 DELL: %content"
}
